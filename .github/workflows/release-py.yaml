# TODO: Enable when PyPI project is created.
# Uncomment the on: block below and remove the workflow_dispatch-only trigger.
# Set up trusted publishing at: https://pypi.org/manage/project/shinyreact/settings/publishing/
#
# on:
#   release:
#     types: [published]

name: release-py

on:
  workflow_dispatch:

env:
  UV_VERSION: "0.9.x"

jobs:
  py-release:
    name: Build and release Python package
    runs-on: ubuntu-latest

    # Python release tags start with "py/v*"
    if: startsWith(github.ref, 'refs/tags/py/v')

    environment:
      name: pypi
      url: https://pypi.org/project/shinyreact/

    permissions:
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - name: 🚀 Install uv
        uses: astral-sh/setup-uv@v6.1.0
        with:
          version: ${{ env.UV_VERSION }}

      - name: 🧳 Build package
        run: uv build

      - name: 🚢 Publish release on PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
