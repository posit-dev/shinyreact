% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/page.R
\name{page_react}
\alias{page_react}
\title{Create a React page from conventional assets — no HTML file required}
\usage{
page_react(
  ...,
  src_dir = "www",
  js_file = "ui.js",
  css_file = "ui.css",
  title = NULL,
  lang = "en",
  shinyreact_js = "server"
)
}
\arguments{
\item{...}{Extra children or \link[htmltools:htmlDependency]{htmltools::htmlDependency} objects.}

\item{src_dir}{Directory containing the assets. Defaults to \code{"www"},
relative to the working directory.}

\item{js_file}{JS entry filename within \code{src_dir}. Defaults to \code{"ui.js"}.}

\item{css_file}{CSS filename within \code{src_dir}. Defaults to \code{"ui.css"}.}

\item{title}{Page title. Defaults to the app folder's name (\code{src_dir}'s
parent when \code{src_dir} is a \code{www} directory), or \code{"shinyreact-app"} when
that resolves to nothing usable (a missing \code{src_dir} is not an error —
the bundle may not be built yet).}

\item{lang}{HTML \code{lang} attribute.}

\item{shinyreact_js}{Who supplies \code{shinyreact.js} (and \code{shinyreact.css}) to
the page. \code{"server"} (the default) serves them from the shinyreact package
as an \link[htmltools:htmlDependency]{htmltools::htmlDependency} — what a no-build app needs, and what
makes \code{window.shinyreact} exist. \code{"client"} is for an app whose own bundle
imports \verb{@posit/shinyreact} and therefore ships its own copy; serving them
too would put two copies of React and the hooks on one page. The
\verb{#shinyreact-config} tag is emitted either way — it carries the protocol
version and any bookmark restore payload. Mirrors Python's
\code{page_react(shinyreact_js=)}.}
}
\value{
UI suitable for \code{shinyApp(ui = ...)}.
}
\description{
The zero-configuration page for the \code{ui.tsx} pattern: the server emits no
body HTML at all. Attaches the shinyreact bundle plus your app's entry
assets, discovered at \code{www/ui.js} and \code{www/ui.css} (relative to the working
directory — the app directory under \code{shiny::runApp()}). Your JS owns the
DOM: create and append your own mount container, e.g.
\code{ReactDOM.createRoot(document.body.appendChild(document.createElement("div")))}.
}
\details{
\code{ui.js} is required (a missing file warns, pointing at the resolved path);
\code{ui.css} is attached only when it exists. Both are served as an
\link[htmltools:htmlDependency]{htmltools::htmlDependency} versioned by \code{ui.js}'s mtime, so the browser
re-fetches after every edit — unlike raw \verb{<script src=...>} tags in a
hand-written HTML file, which the browser caches.
}
