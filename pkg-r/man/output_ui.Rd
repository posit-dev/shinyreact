% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output-ui.R
\name{output_ui}
\alias{output_ui}
\title{Extract the output UI for a Shiny render function}
\usage{
output_ui(render_fn, id)
}
\arguments{
\item{render_fn}{A Shiny render function (e.g. from \code{\link[shiny:renderText]{shiny::renderText()}},
\code{plotly::renderPlotly()}).}

\item{id}{Output id to build the UI for.}
}
\value{
The output UI (a \code{shiny.tag}).
}
\description{
Builds the HTML that a render function's matching \verb{*Output()} function
would produce — e.g. \code{output_ui(renderText(...), "x")} is
\code{textOutput("x")} — without you having to know which output function that
is. The render function's expression is never evaluated; only its UI
constructor runs.
}
\details{
This is the same \code{outputFunc} / \code{outputArgs} contract shiny itself uses in
\code{knit_print} and Express mode, and the R counterpart of the Python
renderer's \code{auto_output_ui()}. Its main use in shinyreact is dependency
extraction: \code{htmltools::findDependencies(output_ui(fn, id))} yields the
\code{htmlDependency} objects (binding JS/CSS) the output needs in the browser —
shinyreact does this automatically for every output registered in a live
session (see \code{install_dep_discovery()}).
}
