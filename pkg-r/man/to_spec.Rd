% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/to_spec.R
\name{to_spec}
\alias{to_spec}
\title{Normalize a value to the shinyreact wire shape}
\usage{
to_spec(x, ...)
}
\arguments{
\item{x}{A \code{Node}, \code{Element}, \code{Spec}, or any JSON-serializable value.}

\item{...}{Further arguments passed to methods.}
}
\value{
A plain list (wire shape) or the input unchanged.
}
\description{
Converts a \link{Node}, \link{Element}, or \link{Spec} to a plain nested list of the form
\verb{list(root = <key>, elements = <named list>)}. Unclassed inputs (plain
lists, vectors) pass through unchanged, so any JSON-serializable value can
be returned from \code{\link[=render_reactive]{render_reactive()}}.
}
\details{
Downstream packages register methods on their own S7 classes:
\code{S7::method(to_spec, MyComponent) <- function(x) { ... }}.
}
