% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wire-tap.R
\name{wire_tap}
\alias{wire_tap}
\title{Tap the websocket wire of a shinytest2 app}
\usage{
wire_tap(app)
}
\arguments{
\item{app}{A \link[shinytest2:AppDriver]{shinytest2::AppDriver} started with
\code{options = list(shiny.trace = TRUE)} — or any object whose \verb{$get_logs()}
returns a data frame with \code{location}, \code{level}, and \code{message} columns in
shinytest2's shape.}
}
\value{
A list of functions:
\describe{
\item{\code{all_output_values(output_id)}}{Every value the server delivered
for \code{output_id}, in order.}
\item{\code{all_messages(message_id)}}{Every \code{\link[=send_message]{send_message()}} payload of
\code{message_id}, in order.}
\item{\code{all_input_values(input_id)}}{Every value the client sent for
\code{input_id}, in order. Matches the bare id or any \code{id:type} wire id
(e.g. the implicit \verb{:shinyreact.default} suffix), so use the id you
wrote in \code{useShinyInput()}.}
\item{\code{expect_output_value(output_id, matcher, timeout = 10)}}{Retrying
expectation. A function \code{matcher} is satisfied by a truthy return; any
other object is compared with \code{\link[=identical]{identical()}}. Returns the matched value
invisibly, or errors at \code{timeout} (seconds). A matcher that errors on a
value's shape counts as a non-match.}
\item{\code{expect_message(message_id, matcher, timeout = 10)}}{As above,
for \code{\link[=send_message]{send_message()}} payloads.}
\item{\code{expect_input_value(input_id, matcher, timeout = 10)}}{As above,
for client-sent input values.}
}
}
\description{
\code{wire_tap()} gives tests access to the JSON payloads that actually crossed
the Shiny websocket — the contract between the server and the React client.
Create the \link[shinytest2:AppDriver]{shinytest2::AppDriver} with \code{options = list(shiny.trace = TRUE)}
so shinytest2 records every websocket frame in \code{app$get_logs()}; the tap
parses those frames into per-channel views.
}
\details{
Cross-channel frame order (which output lands first, how outputs batch into
a single \code{values} frame, busy/progress interleaving) is reactive-scheduling
coincidence, not contract — so the tap deliberately does not expose a global
frame stream. Within one channel (one output id, one message type, one
input id) wire order is guaranteed, and the \verb{expect_*} methods consume it
through a cursor: each expectation scans the recorded history from just
past the previous match, so values that arrive between checks are never
missed — capture is lossless; polling only decides when to re-scan.
Successive expectations on one channel therefore assert an ordered
subsequence.

The Python counterpart is \code{shinyreact.playwright.WireTap}, with the same
methods and semantics. One small divergence: \code{\link[jsonlite:fromJSON]{jsonlite::fromJSON()}} maps a
JSON \code{null} output value to \code{NULL}, indistinguishable from an absent key,
so early \code{output: null} frames are dropped where Python sees \code{None}.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
app <- shinytest2::AppDriver$new(
  "path/to/app",
  options = list(shiny.trace = TRUE)
)
tap <- wire_tap(app)

# The JSON 30 parses to integer; identical() needs the right type.
tap$expect_input_value("bins", 30L)
tap$expect_output_value("dist_data", function(d) {
  d$breaks[[1]] == 43 && sum(unlist(d$counts)) == 272
})
\dontshow{\}) # examplesIf}
}
