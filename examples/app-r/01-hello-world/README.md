# 01 — Hello World (app.R pattern)

R port of `examples/app-py/01-hello-world`. The server describes the UI as a
`node()` tree rendered by registered React components in `hello_world.js`.

## Run

```r
shiny::runApp("examples/app-r/01-hello-world")
```
