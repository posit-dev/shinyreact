import "@/index.css";

import App from "@/App";

const { React, ReactDOM } = window.shinyjson;

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(React.createElement(App));
