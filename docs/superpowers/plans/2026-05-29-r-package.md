# R Package (`pkg-r`) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Promote `pkg-r/` from a placeholder (only built JS/CSS assets) to a working R package mirroring the Python `shinyreact` API surface — `app.R` pattern (S7 `node()`/`spec()`/`element()` + `page_react()` + `render_reactive()`) and `ui.tsx` pattern (`page_react_html()` + raw-JSON renderer) — plus bookmarking and mtime dep versioning.

**Architecture:** S7 classes (`Spec`/`Element`/`Node`) with snake_case constructors model the component tree; a single `to_spec()` S7 generic normalizes any value to a plain nested R list (the wire shape); `render_reactive()` returns that list and lets Shiny serialize it (same as Python's `transform`). Page helpers attach a page-level htmlDependency (bundle + bookmark restore `<script>`); `ui_output()` attaches the bare bundle dep. Bookmark restore reads R Shiny's restore context via `shiny:::` internals isolated behind one wrapper.

**Tech Stack:** R, S7, shiny, htmltools, jsonlite, cli, testthat (3e), roxygen2, air (formatter). Spec: `docs/superpowers/specs/2026-05-26-r-package-design.md`.

**Parity bar correction (vs spec wording):** Cross-language parity tests compare **parsed JSON structures (semantic equality)**, not raw bytes — Python `json.dumps` and R `jsonlite::toJSON` differ in whitespace by design. The spec's "byte-for-byte" / "byte-equality" phrasing is superseded by this plan; a task updates the spec wording.

**Render path correction (vs spec):** `render_reactive()` returns the plain list from `to_spec()`; Shiny serializes the output. No custom `toJSON` is on the output critical path. The `toJSON`/wire-serialization helper exists for tests and the bookmark payload only.

---

## File Structure

**Package metadata / infra:**
- `pkg-r/DESCRIPTION` — package metadata, Imports, `shiny` version floor (bookmark internals)
- `pkg-r/NAMESPACE` — generated by roxygen2 (`devtools::document()`); never hand-edited
- `pkg-r/.Rbuildignore` — exclude non-package files
- `pkg-r/tests/testthat.R` — testthat entry point
- `pkg-r/_pkgdown.yml` — minimal pkgdown config
- `pkg-r/README.md` — minimal install + hello-world

**Source (`pkg-r/R/`), one responsibility each:**
- `spec.R` — S7 classes `Spec`/`Element`/`Node`; constructors `spec()`/`element()`/`node()`; validators
- `to_spec.R` — `to_spec()` S7 generic + methods (`Node`, `Element`, `Spec`, default); `.wire_json()` internal serializer
- `dep.R` — `shinyreact_dep()` (bare bundle), `shinyreact_dep_page()` (bundle + restore script); mtime version
- `output.R` — `ui_output(id, extra_deps)`
- `render.R` — `render_reactive()`
- `message.R` — `send_message(session, type, data)`
- `bookmark.R` — `restore_script_tag()` + `.restore_input_values()` wrapper around `shiny:::`
- `page.R` — `page_bare()`, `page_react()`, `page_react_html()`, `page_react_dep()`
- `shinyreact-package.R` — package-level roxygen `@keywords internal` doc + imports

**Tests (`pkg-r/tests/testthat/`):**
- `test-spec.R`, `test-to-spec.R`, `test-wire-json.R`, `test-dep.R`, `test-ui-output.R`, `test-render.R`, `test-send-message.R`, `test-bookmark.R`, `test-page.R`, `test-parity.R`

**Cross-language fixtures:**
- `pkg-py/tests/fixtures/wire_format/*.json` — committed canonical wire outputs (Python generates)
- `pkg-py/tests/test_wire_fixtures.py` — asserts Python still emits the committed fixtures
- `pkg-r/tests/testthat/fixtures/` — symlink-free copy reference resolved at test time from the installed/loaded path (see Task 12)

**Examples:**
- `examples/app-r/01-hello-world/{app.R,hello_world.js,styles.css,README.md}`
- `examples/app-r/02-inputs/{app.R,inputs.js,styles.css,README.md}`
- `examples/app-r/04-messages/{app.R,messages.js,styles.css,README.md}`
- `examples/ui-tsx-r/01-hello/{app.R,www/index.html,README.md}`

**Docs:**
- `docs/features.md` (modify), `README.md` (modify), `docs/superpowers/specs/2026-05-26-r-package-design.md` (modify: parity wording)

---

## Conventions for every task

- **Run a single test file:** `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-spec.R")'`
- **Format before commit:** `air format pkg-r/` (from repo root, via `make r-format`)
- **Regenerate docs/NAMESPACE after roxygen changes:** `cd pkg-r && Rscript -e 'devtools::document()'`
- **Commit cadence:** one commit per task (after its tests pass). Branch is `schloerke/r-package-issue`.
- **S7 note:** classes are created with `S7::new_class`; the constructor *is* the class object. Exported snake_case constructors are thin wrappers so user code stays snake_case. S7 must be imported and `methods_register()` is not needed when using roxygen `@import S7` patterns — but we call `S7::method()` at load via `.onLoad`-time registration handled by S7 automatically when methods are defined at top level in package code.

---

## Task 1: Package skeleton (DESCRIPTION, infra, loadable empty package)

**Files:**
- Create: `pkg-r/DESCRIPTION`
- Create: `pkg-r/R/shinyreact-package.R`
- Create: `pkg-r/tests/testthat.R`
- Create: `pkg-r/.Rbuildignore`
- Delete: `pkg-r/R/.gitkeep`

- [ ] **Step 1: Write `pkg-r/DESCRIPTION`**

```
Package: shinyreact
Title: Shiny UI Infrastructure for JSON-Driven React Rendering
Version: 0.0.0.9000
Authors@R:
    person("Posit Software, PBC", role = c("aut", "cre"),
           email = "info@posit.co")
Description: Server-side plumbing for rendering React component trees from
    Shiny. Provides the 'app.R' pattern (UI described as R objects) and the
    'ui.tsx' pattern (UI defined in a client React bundle). Ships no UI
    components; downstream packages register their own.
License: MIT + file LICENSE
Encoding: UTF-8
Roxygen: list(markdown = TRUE)
RoxygenNote: 7.3.2
Imports:
    cli,
    htmltools,
    jsonlite,
    S7,
    shiny (>= 1.9.0)
Suggests:
    testthat (>= 3.0.0)
Config/testthat/edition: 3
URL: https://github.com/posit-dev/shinyreact
BugReports: https://github.com/posit-dev/shinyreact/issues
```

- [ ] **Step 2: Write `pkg-r/R/shinyreact-package.R`**

```r
#' @keywords internal
"_PACKAGE"

## usethis namespace: start
#' @import S7
## usethis namespace: end
NULL
```

- [ ] **Step 3: Write `pkg-r/tests/testthat.R`**

```r
library(testthat)
library(shinyreact)

test_check("shinyreact")
```

- [ ] **Step 4: Write `pkg-r/.Rbuildignore`**

```
^_pkgdown\.yml$
^docs$
^pkgdown$
^\.air\.toml$
^LICENSE\.md$
```

- [ ] **Step 5: Create `pkg-r/LICENSE`**

```
YEAR: 2026
COPYRIGHT HOLDER: Posit Software, PBC
```

- [ ] **Step 6: Remove the placeholder**

Run: `git rm pkg-r/R/.gitkeep`

- [ ] **Step 7: Generate NAMESPACE and verify the package loads**

Run: `cd pkg-r && Rscript -e 'devtools::document(); devtools::load_all(".")'`
Expected: loads with no errors; `NAMESPACE` created (contains at least `import(S7)`).

- [ ] **Step 8: Commit**

```bash
air format pkg-r/
git add pkg-r/DESCRIPTION pkg-r/R pkg-r/NAMESPACE pkg-r/tests pkg-r/.Rbuildignore pkg-r/LICENSE
git commit -m "feat(r): package skeleton — DESCRIPTION, infra, loadable empty package (#91)"
```

---

## Task 2: S7 data model — `element()`, `spec()`, `node()`

**Files:**
- Create: `pkg-r/R/spec.R`
- Create: `pkg-r/tests/testthat/test-spec.R`

- [ ] **Step 1: Write the failing tests in `pkg-r/tests/testthat/test-spec.R`**

```r
test_that("element() builds an Element with defaults", {
  el <- element("TextInput")
  expect_s3_class(el, "shinyreact::Element")
  expect_identical(el@type, "TextInput")
  expect_identical(el@props, list())
  expect_identical(el@children, list())
})

test_that("element() rejects a non-scalar or empty type", {
  expect_error(element(""), "non-empty")
  expect_error(element(c("a", "b")), "single")
})

test_that("element() rejects unnamed props", {
  expect_error(element("X", props = list(1, 2)), "named")
})

test_that("element() children must be length-1 character keys", {
  expect_error(element("X", children = list(1)), "character")
  ok <- element("X", children = list("k1", "k2"))
  expect_identical(ok@children, list("k1", "k2"))
})

test_that("spec() requires root to be present in elements", {
  els <- list(a = element("Card"))
  s <- spec("a", els)
  expect_s3_class(s, "shinyreact::Spec")
  expect_error(spec("missing", els), "root")
})

test_that("node() collects children via ...", {
  n <- node("Card", node("Divider"), props = list(title = "Hi"))
  expect_s3_class(n, "shinyreact::Node")
  expect_identical(n@type, "Card")
  expect_identical(n@props, list(title = "Hi"))
  expect_length(n@children, 1)
  expect_s3_class(n@children[[1]], "shinyreact::Node")
})

test_that("node() children must be Node objects", {
  expect_error(node("Card", "text"), "Node")
  expect_error(node("Card", 42), "Node")
})
```

- [ ] **Step 2: Run to verify failure**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-spec.R")'`
Expected: FAIL — `could not find function "element"`.

- [ ] **Step 3: Write `pkg-r/R/spec.R`**

```r
#' Component tree data model
#'
#' `element()`, `spec()`, and `node()` construct the data sent to the browser.
#' `Element`/`Spec` are the flat wire model; `Node` is the nested authoring
#' model that flattens to a `Spec` via [to_spec()].
#'
#' @param type Component type string (e.g. `"Card"`). Single non-empty string.
#' @param props Named list of props. May be empty.
#' @param children For `element()`, a list of length-1 character element keys.
#'   For `node()`, passed via `...` and must be `Node` objects.
#' @param root For `spec()`, the key in `elements` to render first.
#' @param elements For `spec()`, a named list of `Element` objects.
#' @param ... For `node()`, child `Node` objects.
#'
#' @return An S7 object of class `Element`, `Spec`, or `Node`.
#' @name data-model
NULL

.check_type <- function(type, call = rlang::caller_env()) {
  if (!is.character(type) || length(type) != 1L) {
    return("@type must be a single string")
  }
  if (!nzchar(type)) {
    return("@type must be a non-empty string")
  }
  NULL
}

.check_named_list <- function(x, what) {
  if (!is.list(x)) {
    return(sprintf("@%s must be a list", what))
  }
  if (length(x) > 0L) {
    nms <- names(x)
    if (is.null(nms) || any(!nzchar(nms))) {
      return(sprintf("@%s must be a named list", what))
    }
  }
  NULL
}

#' @rdname data-model
#' @export
Element <- S7::new_class(
  "Element",
  properties = list(
    type = S7::class_character,
    props = S7::class_list,
    children = S7::class_list
  ),
  constructor = function(type, props = list(), children = list()) {
    S7::new_object(S7::S7_object(), type = type, props = props, children = children)
  },
  validator = function(self) {
    msg <- .check_type(self@type)
    if (!is.null(msg)) return(msg)
    msg <- .check_named_list(self@props, "props")
    if (!is.null(msg)) return(msg)
    if (!is.list(self@children)) return("@children must be a list")
    ok <- vapply(
      self@children,
      function(c) is.character(c) && length(c) == 1L,
      logical(1)
    )
    if (length(ok) && !all(ok)) {
      return("@children must be a list of length-1 character element keys")
    }
    NULL
  }
)

#' @rdname data-model
#' @export
Spec <- S7::new_class(
  "Spec",
  properties = list(
    root = S7::class_character,
    elements = S7::class_list
  ),
  validator = function(self) {
    if (!is.character(self@root) || length(self@root) != 1L) {
      return("@root must be a single string")
    }
    if (!(self@root %in% names(self@elements))) {
      return(sprintf(
        "@root '%s' not found in elements keys: %s",
        self@root,
        paste(names(self@elements), collapse = ", ")
      ))
    }
    NULL
  }
)

#' @rdname data-model
#' @export
Node <- S7::new_class(
  "Node",
  properties = list(
    type = S7::class_character,
    props = S7::class_list,
    children = S7::class_list
  ),
  validator = function(self) {
    msg <- .check_type(self@type)
    if (!is.null(msg)) return(msg)
    msg <- .check_named_list(self@props, "props")
    if (!is.null(msg)) return(msg)
    if (!is.list(self@children)) return("@children must be a list")
    ok <- vapply(self@children, function(c) S7::S7_inherits(c, Node), logical(1))
    if (length(ok) && !all(ok)) {
      return("node() children must be Node objects")
    }
    NULL
  }
)

#' @rdname data-model
#' @export
element <- function(type, props = list(), children = list()) {
  Element(type = type, props = props, children = children)
}

#' @rdname data-model
#' @export
spec <- function(root, elements) {
  Spec(root = root, elements = elements)
}

#' @rdname data-model
#' @export
node <- function(type, ..., props = list()) {
  children <- list(...)
  bad <- which(!vapply(children, function(c) S7::S7_inherits(c, Node), logical(1)))
  if (length(bad)) {
    cli::cli_abort(c(
      "`node()` children must be {.cls Node} objects.",
      "x" = "Children at position{?s} {bad} {?is/are} not a Node.",
      "i" = "Pass text via {.arg props} (e.g. props = list(text = \"Hello\")); the registered React component renders it."
    ))
  }
  Node(type = type, props = props, children = children)
}
```

Note: `rlang` is referenced only via `rlang::caller_env()` default in `.check_type`; drop that arg if you prefer not to add `rlang` — replace the signature with `.check_type <- function(type)`. To avoid the dependency, **remove the `call`/`rlang` reference** (it is unused in the body). Final `.check_type` signature: `function(type)`.

- [ ] **Step 4: Apply the rlang-free `.check_type`**

Edit `pkg-r/R/spec.R`: change `.check_type <- function(type, call = rlang::caller_env()) {` to `.check_type <- function(type) {`.

- [ ] **Step 5: Run tests to verify pass**

Run: `cd pkg-r && Rscript -e 'devtools::document(); devtools::load_all("."); testthat::test_file("tests/testthat/test-spec.R")'`
Expected: PASS (all tests).

- [ ] **Step 6: Commit**

```bash
air format pkg-r/
cd pkg-r && Rscript -e 'devtools::document()' && cd ..
git add pkg-r/R/spec.R pkg-r/tests/testthat/test-spec.R pkg-r/NAMESPACE pkg-r/man
git commit -m "feat(r): S7 data model — element(), spec(), node() (#91)"
```

---

## Task 3: `to_spec()` generic + methods

**Files:**
- Create: `pkg-r/R/to_spec.R`
- Create: `pkg-r/tests/testthat/test-to-spec.R`

`to_spec()` converts any value to the **plain nested wire list** with shape `list(root=, elements=)`. `Node` is flattened with auto keys (`auto_001`, depth-first); `Element`/`Spec` are normalized to plain lists; unclassed input passes through unchanged.

- [ ] **Step 1: Write failing tests `pkg-r/tests/testthat/test-to-spec.R`**

```r
test_that("to_spec() passes plain lists through unchanged", {
  expect_identical(to_spec(list(a = 1)), list(a = 1))
  expect_identical(to_spec("hi"), "hi")
  expect_identical(to_spec(42L), 42L)
})

test_that("to_spec(node) flattens depth-first with auto keys", {
  n <- node("Card", node("Divider"), node("Text", props = list(value = "x")))
  out <- to_spec(n)
  expect_identical(out$root, "auto_001")
  expect_named(out$elements, c("auto_001", "auto_002", "auto_003"))
  expect_identical(out$elements$auto_001$type, "Card")
  expect_identical(out$elements$auto_001$children, list("auto_002", "auto_003"))
  expect_identical(out$elements$auto_002$type, "Divider")
  expect_identical(out$elements$auto_003$props, list(value = "x"))
})

test_that("to_spec(spec) normalizes to a plain list", {
  s <- spec("a", list(a = element("Card", props = list(title = "Hi"))))
  out <- to_spec(s)
  expect_identical(out$root, "a")
  expect_identical(out$elements$a$type, "Card")
  expect_identical(out$elements$a$props, list(title = "Hi"))
  expect_identical(out$elements$a$children, list())
})

test_that("to_spec(element) wraps a single element into a spec list", {
  out <- to_spec(element("Card"))
  expect_length(out$elements, 1)
  expect_identical(out$elements[[out$root]]$type, "Card")
})
```

- [ ] **Step 2: Run to verify failure**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-to-spec.R")'`
Expected: FAIL — `could not find function "to_spec"`.

- [ ] **Step 3: Write `pkg-r/R/to_spec.R`**

```r
#' Normalize a value to the shinyreact wire shape
#'
#' Converts a [Node], [Element], or [Spec] to a plain nested list of the form
#' `list(root = <key>, elements = <named list>)`. Unclassed inputs (plain
#' lists, vectors) pass through unchanged, so any JSON-serializable value can
#' be returned from [render_reactive()].
#'
#' Downstream packages register methods on their own S7 classes:
#' `S7::method(to_spec, MyComponent) <- function(x) { ... }`.
#'
#' @param x A `Node`, `Element`, `Spec`, or any JSON-serializable value.
#' @return A plain list (wire shape) or the input unchanged.
#' @export
to_spec <- S7::new_generic("to_spec", "x")

.element_to_list <- function(el) {
  list(type = el@type, props = el@props, children = el@children)
}

S7::method(to_spec, Element) <- function(x) {
  key <- "auto_001"
  els <- list()
  els[[key]] <- .element_to_list(x)
  list(root = key, elements = els)
}

S7::method(to_spec, Spec) <- function(x) {
  els <- lapply(x@elements, .element_to_list)
  list(root = x@root, elements = els)
}

S7::method(to_spec, Node) <- function(x) {
  elements <- list()
  counter <- 0L

  walk <- function(n) {
    counter <<- counter + 1L
    key <- sprintf("auto_%03d", counter)
    child_keys <- lapply(n@children, walk)
    elements[[key]] <<- list(
      type = n@type,
      props = n@props,
      children = child_keys
    )
    key
  }

  root_key <- walk(x)
  list(root = root_key, elements = elements)
}

S7::method(to_spec, S7::class_any) <- function(x) {
  x
}
```

- [ ] **Step 4: Run tests to verify pass**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-to-spec.R")'`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
air format pkg-r/
cd pkg-r && Rscript -e 'devtools::document()' && cd ..
git add pkg-r/R/to_spec.R pkg-r/tests/testthat/test-to-spec.R pkg-r/NAMESPACE pkg-r/man
git commit -m "feat(r): to_spec() generic flattens Node/Element/Spec to wire list (#91)"
```

---

## Task 4: Wire JSON serializer `.wire_json()`

**Files:**
- Create: `pkg-r/R/to_spec.R` (append)
- Create: `pkg-r/tests/testthat/test-wire-json.R`

The serializer is used by the bookmark payload and parity tests. It must: unbox scalars, emit empty `children`/arrays as `[]`, and emit empty `props` as `{}` (objects), matching the JS renderer's expectations.

- [ ] **Step 1: Write failing tests `pkg-r/tests/testthat/test-wire-json.R`**

```r
test_that(".wire_json unboxes scalars and emits arrays for children", {
  s <- to_spec(node("Card", node("Divider")))
  txt <- shinyreact:::.wire_json(s)
  parsed <- jsonlite::fromJSON(txt, simplifyVector = FALSE)
  expect_identical(parsed$root, "auto_001")
  expect_identical(parsed$elements$auto_001$type, "Card")
  expect_identical(parsed$elements$auto_001$children, list("auto_002"))
  expect_identical(parsed$elements$auto_002$children, list())
})

test_that(".wire_json emits empty props as a JSON object, not array", {
  s <- to_spec(element("Divider"))
  txt <- shinyreact:::.wire_json(s)
  # Empty object renders as {}; empty array as [].
  expect_match(txt, '"props":\\{\\}')
})

test_that(".wire_json round-trips a string value unchanged", {
  txt <- shinyreact:::.wire_json("HELLO")
  expect_identical(jsonlite::fromJSON(txt), "HELLO")
})
```

- [ ] **Step 2: Run to verify failure**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-wire-json.R")'`
Expected: FAIL — `.wire_json` not found.

- [ ] **Step 3: Append `.wire_json()` to `pkg-r/R/to_spec.R`**

```r
# Force a list that should serialize as a JSON object (even when empty).
# jsonlite emits an unnamed empty list as `[]`; tagging with names = character()
# and the "names" attribute makes it `{}`.
.as_json_object <- function(x) {
  if (is.list(x) && length(x) == 0L) {
    return(structure(list(), names = character()))
  }
  x
}

# Recursively mark `props` sub-lists so empty ones serialize as objects.
.mark_objects <- function(node) {
  if (!is.list(node)) {
    return(node)
  }
  if (!is.null(node$props)) {
    node$props <- .as_json_object(node$props)
  }
  if (!is.null(node$elements)) {
    node$elements <- lapply(node$elements, .mark_objects)
  }
  node
}

#' @keywords internal
.wire_json <- function(x) {
  payload <- .mark_objects(x)
  jsonlite::toJSON(
    payload,
    auto_unbox = TRUE,
    null = "null",
    na = "null"
  )
}
```

- [ ] **Step 4: Run tests to verify pass**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-wire-json.R")'`
Expected: PASS. If the empty-object test fails (jsonlite version differences), adjust `.as_json_object` to `jsonlite::toJSON(setNames(list(), character()))` verified to produce `{}` on the installed jsonlite; the test is the source of truth.

- [ ] **Step 5: Commit**

```bash
air format pkg-r/
git add pkg-r/R/to_spec.R pkg-r/tests/testthat/test-wire-json.R
git commit -m "feat(r): .wire_json() serializer with empty-object handling (#91)"
```

---

## Task 5: `shinyreact_dep()` + `shinyreact_dep_page()` (mtime version)

**Files:**
- Create: `pkg-r/R/dep.R`
- Create: `pkg-r/tests/testthat/test-dep.R`

`shinyreact_dep_page()` references `restore_script_tag()` from Task 8. To keep tasks independently testable, define a stub `restore_script_tag()` here that returns `NULL`; Task 8 replaces the stub with the real implementation in `bookmark.R` and removes the stub.

- [ ] **Step 1: Write failing tests `pkg-r/tests/testthat/test-dep.R`**

```r
test_that("shinyreact_dep() points at the bundled assets", {
  dep <- shinyreact:::shinyreact_dep()
  expect_s3_class(dep, "html_dependency")
  expect_identical(dep$name, "shinyreact")
  expect_identical(dep$script[[1]]$src %||% dep$script, "shinyreact.js")
})

test_that("shinyreact_dep() version is a non-empty string", {
  dep <- shinyreact:::shinyreact_dep()
  expect_true(is.character(dep$version) && nzchar(dep$version))
})

test_that("shinyreact_dep_page() returns the bundle dep when no restore context", {
  out <- shinyreact:::shinyreact_dep_page()
  # With no restore script, it is just the dependency (or a tagList wrapping it).
  expect_true(inherits(out, "html_dependency") || inherits(out, "shiny.tag.list"))
})
```

Add a small `%||%` helper at the top of the test file if not available:

```r
`%||%` <- function(a, b) if (is.null(a)) b else a
```

- [ ] **Step 2: Run to verify failure**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-dep.R")'`
Expected: FAIL — `shinyreact_dep` not found.

- [ ] **Step 3: Write `pkg-r/R/dep.R`**

```r
.www_dir <- function() {
  system.file("lib", "shiny", package = "shinyreact")
}

.bundle_version <- function() {
  js <- file.path(.www_dir(), "shinyreact.js")
  mtime <- suppressWarnings(file.mtime(js))
  if (length(mtime) == 1L && !is.na(mtime)) {
    return(as.character(as.integer(mtime)))
  }
  as.character(utils::packageVersion("shinyreact"))
}

# Internal: bare bundle dependency for per-output consumers.
shinyreact_dep <- function() {
  htmltools::htmlDependency(
    name = "shinyreact",
    version = .bundle_version(),
    src = c(file = .www_dir()),
    script = list(src = "shinyreact.js", defer = ""),
    stylesheet = "shinyreact.css"
  )
}

# Stub replaced in bookmark.R (Task 8). Returns NULL when no restore context.
restore_script_tag <- function() {
  NULL
}

# Internal: page-level dependency = bundle + bookmark restore <script> (if any).
shinyreact_dep_page <- function() {
  restore <- restore_script_tag()
  if (is.null(restore)) {
    return(shinyreact_dep())
  }
  htmltools::tagList(shinyreact_dep(), restore)
}
```

`utils` is in `R` core; add `importFrom(utils, packageVersion)` by adding `#' @importFrom utils packageVersion` to `shinyreact-package.R`. For this task, simplest: reference `utils::packageVersion` fully-qualified (already done) — no NAMESPACE change needed since `utils` is a base-priority package always attached. Leave as fully-qualified.

- [ ] **Step 4: Run tests to verify pass**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-dep.R")'`
Expected: PASS. (The bundle exists at `pkg-r/inst/lib/shiny/shinyreact.js`, so the mtime path is exercised.)

- [ ] **Step 5: Commit**

```bash
air format pkg-r/
git add pkg-r/R/dep.R pkg-r/tests/testthat/test-dep.R
git commit -m "feat(r): shinyreact_dep() + page dep with mtime version (#91)"
```

---

## Task 6: `ui_output()`

**Files:**
- Create: `pkg-r/R/output.R`
- Create: `pkg-r/tests/testthat/test-ui-output.R`

- [ ] **Step 1: Write failing tests `pkg-r/tests/testthat/test-ui-output.R`**

```r
test_that("ui_output() builds a shinyreact-output div with the dep", {
  tag <- ui_output("hello")
  expect_s3_class(tag, "shiny.tag")
  expect_identical(tag$attribs$id, "hello")
  expect_identical(tag$attribs$class, "shinyreact-output")
  deps <- htmltools::findDependencies(tag)
  expect_true(any(vapply(deps, function(d) d$name == "shinyreact", logical(1))))
})

test_that("ui_output() includes extra_deps", {
  extra <- htmltools::htmlDependency("mydep", "1.0.0", src = c(href = "x"))
  tag <- ui_output("hello", extra_deps = list(extra))
  deps <- htmltools::findDependencies(tag)
  names <- vapply(deps, function(d) d$name, character(1))
  expect_true(all(c("shinyreact", "mydep") %in% names))
})
```

- [ ] **Step 2: Run to verify failure**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-ui-output.R")'`
Expected: FAIL — `ui_output` not found.

- [ ] **Step 3: Write `pkg-r/R/output.R`**

```r
#' Output placeholder for a shinyreact renderer
#'
#' Creates the `<div>` that the shinyreact Shiny output binding renders into.
#' Pair with [render_reactive()] on the server (assign to `output[[id]]`).
#'
#' @param id Output ID. Must match the server-side `output[[id]]` assignment.
#' @param extra_deps A list of [htmltools::htmlDependency] objects to include.
#'   Downstream packages use this to inject their own JS/CSS.
#' @return A `shiny.tag` `<div>`.
#' @export
ui_output <- function(id, extra_deps = list()) {
  htmltools::div(
    id = id,
    class = "shinyreact-output",
    shinyreact_dep(),
    !!!extra_deps
  )
}
```

Note: `!!!extra_deps` requires rlang splicing inside `htmltools::div`. htmltools supports `!!!` via rlang. If `rlang` is not a dependency, replace with `do.call`:

```r
ui_output <- function(id, extra_deps = list()) {
  do.call(
    htmltools::div,
    c(
      list(id = id, class = "shinyreact-output", shinyreact_dep()),
      extra_deps
    )
  )
}
```

Use the `do.call` form (no rlang dependency).

- [ ] **Step 4: Run tests to verify pass**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-ui-output.R")'`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
air format pkg-r/
cd pkg-r && Rscript -e 'devtools::document()' && cd ..
git add pkg-r/R/output.R pkg-r/tests/testthat/test-ui-output.R pkg-r/NAMESPACE pkg-r/man
git commit -m "feat(r): ui_output() output placeholder (#91)"
```

---

## Task 7: `render_reactive()`

**Files:**
- Create: `pkg-r/R/render.R`
- Create: `pkg-r/tests/testthat/test-render.R`

Mirrors Shiny's `render*` family via `shiny::createRenderFunction`. The value-transform returns `to_spec(value)` (a plain list) and lets Shiny serialize it. `NULL` returns `NULL`.

- [ ] **Step 1: Write failing tests `pkg-r/tests/testthat/test-render.R`**

```r
test_that("render_reactive transforms a Node into the wire list", {
  r <- render_reactive(node("Card", props = list(title = "Hi")))
  # createRenderFunction returns a function; invoke it to get the value.
  val <- r()
  expect_identical(val$root, "auto_001")
  expect_identical(val$elements$auto_001$type, "Card")
  expect_identical(val$elements$auto_001$props, list(title = "Hi"))
})

test_that("render_reactive passes a raw list through", {
  r <- render_reactive(list(key = "value", count = 42L))
  expect_identical(r(), list(key = "value", count = 42L))
})

test_that("render_reactive returns NULL for NULL", {
  r <- render_reactive(NULL)
  expect_null(r())
})
```

- [ ] **Step 2: Run to verify failure**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-render.R")'`
Expected: FAIL — `render_reactive` not found.

- [ ] **Step 3: Write `pkg-r/R/render.R`**

```r
#' Render a React component tree (or raw data) to a shinyreact output
#'
#' The server-side counterpart to `useShinyOutputValue()` on the React client.
#' Assign to `output[[id]]` where the UI has a matching [ui_output()].
#'
#' Accepts a [node()]/[spec()]/[element()] tree (flattened via [to_spec()]) or
#' any JSON-serializable value (passed through unchanged).
#'
#' @param expr An expression returning a `Node`/`Spec`/`Element` or a
#'   JSON-serializable value.
#' @param env The environment in which to evaluate `expr`.
#' @param quoted Is `expr` already quoted?
#' @return A Shiny render function.
#' @export
render_reactive <- function(expr, env = parent.frame(), quoted = FALSE) {
  func <- shiny::installExprFunction(expr, "func", env, quoted)

  shiny::createRenderFunction(
    func,
    function(value, session, name, ...) {
      if (is.null(value)) {
        return(NULL)
      }
      to_spec(value)
    },
    ui_output
  )
}
```

- [ ] **Step 4: Run tests to verify pass**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-render.R")'`
Expected: PASS. If `createRenderFunction`'s returned function cannot be invoked bare in a test (it may require a reactive context), wrap the invocation in `shiny::isolate(shiny::reactiveConsole(TRUE))` or test the transform function directly by extracting it. Fallback: factor the transform into an internal `.render_transform(value)` and unit-test that directly; keep `render_reactive` as the thin Shiny wrapper.

- [ ] **Step 5: If the fallback was needed, refactor**

If Step 4 required the fallback, edit `pkg-r/R/render.R` to extract:

```r
.render_transform <- function(value) {
  if (is.null(value)) {
    return(NULL)
  }
  to_spec(value)
}
```

and have both `createRenderFunction`'s transform and the tests call `.render_transform`. Update `test-render.R` to call `shinyreact:::.render_transform(...)`.

- [ ] **Step 6: Commit**

```bash
air format pkg-r/
cd pkg-r && Rscript -e 'devtools::document()' && cd ..
git add pkg-r/R/render.R pkg-r/tests/testthat/test-render.R pkg-r/NAMESPACE pkg-r/man
git commit -m "feat(r): render_reactive() Shiny renderer (#91)"
```

---

## Task 8: `send_message()`

**Files:**
- Create: `pkg-r/R/message.R`
- Create: `pkg-r/tests/testthat/test-send-message.R`

Mirrors Python: namespaces `type` via the session's namespace, then sends `"shinyReactMessage"` with `list(type=, data=)`. R's per-session namespace is `session$ns(type)`.

- [ ] **Step 1: Write failing tests `pkg-r/tests/testthat/test-send-message.R`**

```r
test_that("send_message sends a namespaced shinyReactMessage", {
  sent <- NULL
  fake_session <- list(
    ns = function(x) paste0("mod-", x),
    sendCustomMessage = function(type, message) {
      sent <<- list(type = type, message = message)
    }
  )
  send_message(fake_session, "notify", list(text = "hi"))
  expect_identical(sent$type, "shinyReactMessage")
  expect_identical(sent$message$type, "mod-notify")
  expect_identical(sent$message$data, list(text = "hi"))
})

test_that("send_message tolerates a session without ns()", {
  sent <- NULL
  fake_session <- list(
    sendCustomMessage = function(type, message) {
      sent <<- message
    }
  )
  send_message(fake_session, "notify", list(n = 1L))
  expect_identical(sent$type, "notify")
})
```

- [ ] **Step 2: Run to verify failure**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-send-message.R")'`
Expected: FAIL — `send_message` not found.

- [ ] **Step 3: Write `pkg-r/R/message.R`**

```r
#' Send a custom message to client React components
#'
#' Messages are consumed by `useShinyMessageHandler(type, handler)` on the
#' React side. The `type` is namespaced to the current Shiny module (if any)
#' so module-scoped handlers match.
#'
#' @param session The Shiny session.
#' @param type Message type string; must match the `messageType` passed to
#'   `useShinyMessageHandler()` in the React component.
#' @param data Any JSON-serializable data.
#' @return Invisibly `NULL`.
#' @export
send_message <- function(session, type, data) {
  namespaced_type <- if (is.function(session$ns)) session$ns(type) else type
  session$sendCustomMessage(
    "shinyReactMessage",
    list(type = namespaced_type, data = data)
  )
  invisible(NULL)
}
```

- [ ] **Step 4: Run tests to verify pass**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-send-message.R")'`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
air format pkg-r/
cd pkg-r && Rscript -e 'devtools::document()' && cd ..
git add pkg-r/R/message.R pkg-r/tests/testthat/test-send-message.R pkg-r/NAMESPACE pkg-r/man
git commit -m "feat(r): send_message() custom message helper (#91)"
```

---

## Task 9: Bookmark restore — `bookmark.R`

**Files:**
- Create: `pkg-r/R/bookmark.R`
- Modify: `pkg-r/R/dep.R` (remove the stub `restore_script_tag()`)
- Create: `pkg-r/tests/testthat/test-bookmark.R`

`restore_script_tag()` reads the active restore context's input map (non-destructively) and emits a head `<script>` setting `window.shinyreact._restore`. Restore-context access uses `shiny:::` internals isolated in one wrapper, `.restore_input_values()`.

**First sub-step is a SPIKE to confirm the R Shiny internal API** (the design accepts `shiny:::`, but the exact symbol must be confirmed against the installed Shiny).

- [ ] **Step 1: SPIKE — identify the restore-context internal**

Run: `Rscript -e 'library(shiny); ls(getNamespace("shiny"), pattern = "[Rr]estore")'`
Then inspect candidates (likely `RestoreContext`, `restoreCtx`, or a field on the session). Confirm how to read the parsed input values without marking them used. Record the chosen symbol(s) in a comment at the top of `bookmark.R`. Expected outcome: a function/expression that returns a named list of restored input values for the current request, or empty when none.

Reference behavior to mirror (Python): `get_current_restore_context().input.as_dict()` — read the raw map, do **not** call `restore_input()` (which marks used).

- [ ] **Step 2: Write failing tests `pkg-r/tests/testthat/test-bookmark.R`**

```r
test_that("restore_script_tag returns NULL with no restore values", {
  testthat::local_mocked_bindings(
    .restore_input_values = function() list()
  )
  expect_null(restore_script_tag())
})

test_that("restore_script_tag emits a JSON.parse restore script", {
  testthat::local_mocked_bindings(
    .restore_input_values = function() list(name = "Alice", n = 3L)
  )
  tagobj <- restore_script_tag()
  html <- as.character(tagobj)
  expect_match(html, "window\\.shinyreact\\._restore = JSON\\.parse\\(")
  # The payload is a JS string literal; parse out and verify it round-trips.
  expect_match(html, "Alice")
})

test_that("restore script payload survives __proto__ keys", {
  testthat::local_mocked_bindings(
    .restore_input_values = function() stats::setNames(list("x"), "__proto__")
  )
  html <- as.character(restore_script_tag())
  expect_match(html, "__proto__")
})
```

Note: `local_mocked_bindings` requires the bound function to exist in the package namespace; `.restore_input_values` is defined in Step 3.

- [ ] **Step 3: Write `pkg-r/R/bookmark.R`**

```r
# Restore-context access uses Shiny internals (accepted per design spec
# docs/superpowers/specs/2026-05-26-r-package-design.md). The single wrapper
# below is the only place that touches `shiny:::`; if Shiny exposes a public
# API, replace just this function.
#
# SPIKE RESULT (Task 9 Step 1): <fill in the confirmed symbol, e.g.
#   shiny:::RestoreContext or session$restoreContext$asList()>.

#' @keywords internal
.restore_input_values <- function() {
  # Read the parsed restore input values for the current request without
  # marking them used (mirrors Python's ctx.input.as_dict()).
  ctx <- tryCatch(
    shiny:::RestoreContext$new(),  # PLACEHOLDER — replace per spike result
    error = function(e) NULL
  )
  # The real implementation reads the active context off the current domain.
  # If unavailable, return an empty list.
  values <- tryCatch(.read_restore_map(ctx), error = function(e) list())
  if (is.null(values)) list() else values
}

# Helper kept separate so the spike's chosen accessor is isolated.
.read_restore_map <- function(ctx) {
  if (is.null(ctx)) {
    return(list())
  }
  # <fill in per spike: e.g. as.list(ctx$input) WITHOUT consuming values>
  list()
}

#' @keywords internal
restore_script_tag <- function() {
  values <- .restore_input_values()
  if (length(values) == 0L) {
    return(NULL)
  }

  # Layer 1: JSON of the values, with </ neutralized so the embedded JSON
  # cannot close the surrounding <script>.
  json_payload <- gsub("</", "<\\/", .wire_json(values), fixed = TRUE)
  # Layer 2: wrap as a JS string literal (double-encode) so quotes/newlines
  # survive the JS parser before JSON.parse runs.
  js_string_literal <- jsonlite::toJSON(json_payload, auto_unbox = TRUE)

  js <- paste0(
    "window.shinyreact = window.shinyreact || {};",
    "window.shinyreact._restore = JSON.parse(", js_string_literal, ");"
  )

  htmltools::tags$script(htmltools::HTML(js))
}
```

`.wire_json(values)` serializes the named list of input values (unboxing scalars). The `JSON.parse(<string literal>)` wrapper makes `__proto__`/`constructor` keys ordinary data properties — same rationale as Python.

**Important:** `:::` access to an unexported object in *another* package will fail `R CMD check` as an ERROR for `shiny:::RestoreContext$new()` if that is not actually the right symbol. Use the spike result. If the chosen accessor is a function, prefer `utils::getFromNamespace("name", "shiny")` captured once at first use, which is the conventional way to keep the dependency explicit and reviewable.

- [ ] **Step 4: Remove the stub in `dep.R`**

Edit `pkg-r/R/dep.R`: delete the stub `restore_script_tag()` block (the one returning `NULL`), since the real one now lives in `bookmark.R`.

- [ ] **Step 5: Run tests to verify pass**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-bookmark.R")'`
Expected: PASS. The mocked `.restore_input_values` avoids needing a live restore context in unit tests.

- [ ] **Step 6: Commit**

```bash
air format pkg-r/
git add pkg-r/R/bookmark.R pkg-r/R/dep.R pkg-r/tests/testthat/test-bookmark.R
git commit -m "feat(r): bookmark restore_script_tag() via shiny internals (#91)"
```

---

## Task 10: Page helpers — `page_bare()`, `page_react()`, `page_react_html()`, `page_react_dep()`

**Files:**
- Create: `pkg-r/R/page.R`
- Create: `pkg-r/tests/testthat/test-page.R`

- [ ] **Step 1: Write failing tests `pkg-r/tests/testthat/test-page.R`**

```r
test_that("page_react includes #root and the shinyreact dep", {
  ui <- page_react()
  html <- as.character(ui)
  expect_match(html, 'id="root"')
  deps <- htmltools::findDependencies(ui)
  expect_true(any(vapply(deps, function(d) d$name == "shinyreact", logical(1))))
})

test_that("page_bare wraps children without #root", {
  ui <- page_bare(htmltools::div("hi"))
  html <- as.character(ui)
  expect_no_match(html, 'id="root"')
})

test_that("page_react_html reads a plain HTML file and attaches the dep", {
  tmp <- withr::local_tempfile(fileext = ".html")
  writeLines("<!DOCTYPE html><html><body><div id='root'></div></body></html>", tmp)
  ui <- page_react_html(tmp)
  deps <- htmltools::findDependencies(ui)
  expect_true(any(vapply(deps, function(d) d$name == "shinyreact", logical(1))))
})

test_that("page_react_html errors clearly on a missing file", {
  expect_error(page_react_html("does-not-exist.html"), "not found")
})
```

Add `withr` to `Suggests` in `DESCRIPTION` (used in tests only).

- [ ] **Step 2: Run to verify failure**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-page.R")'`
Expected: FAIL — `page_react` not found.

- [ ] **Step 3: Write `pkg-r/R/page.R`**

```r
#' Bare HTML page with Shiny dependencies
#'
#' Escape hatch for custom setups. Wraps [shiny::bootstrapPage()].
#'
#' @param ... Child tags or [htmltools::htmlDependency] objects.
#' @param title Page title.
#' @param lang HTML `lang` attribute.
#' @return A `shiny.tag` page.
#' @export
page_bare <- function(..., title = NULL, lang = "en") {
  shiny::bootstrapPage(..., title = title, lang = lang)
}

#' Full-page React app served by Shiny
#'
#' Creates a page with a `#root` div and the shinyreact page-level dependency
#' (bundle + bookmark restore script). Pass app JS/CSS dependencies (e.g. from
#' [page_react_dep()]) via `...`.
#'
#' @inheritParams page_bare
#' @return A `shiny.tag` page.
#' @export
page_react <- function(..., title = NULL, lang = "en") {
  page_bare(
    shinyreact_dep_page(),
    htmltools::tags$div(id = "root"),
    ...,
    title = title,
    lang = lang
  )
}

#' Serve a static React `index.html` (the `ui.tsx` pattern)
#'
#' Reads an HTML file and attaches the shinyreact page-level dependency. Use as
#' the `ui` argument: `shinyApp(ui = page_react_html(), server = ...)`. Works
#' with plain HTML (no `{{ headContent() }}` template syntax required).
#'
#' @param path Path to the HTML file. Defaults to `"www/index.html"`.
#' @return UI suitable for `shinyApp(ui = ...)`.
#' @export
page_react_html <- function(path = "www/index.html") {
  if (!file.exists(path)) {
    cli::cli_abort("HTML file not found: {.path {path}}")
  }
  html <- htmltools::HTML(paste(readLines(path, warn = FALSE), collapse = "\n"))
  htmltools::attachDependencies(
    htmltools::tagList(html),
    htmltools::findDependencies(shinyreact_dep_page()),
    append = TRUE
  )
}

#' HTML dependency for a downstream package's own JS/CSS bundle
#'
#' Convenience mirroring Python's `page_react_dep()`. Versioned by the JS
#' file's mtime so browsers re-fetch after a rebuild.
#'
#' @param src_dir Directory containing the JS/CSS.
#' @param js_file JS filename within `src_dir`.
#' @param css_file Optional CSS filename within `src_dir`.
#' @param name Dependency name; defaults to `basename(src_dir)`.
#' @return An [htmltools::htmlDependency].
#' @export
page_react_dep <- function(src_dir, js_file, css_file = NULL,
                           name = basename(src_dir)) {
  js_path <- file.path(src_dir, js_file)
  mtime <- suppressWarnings(file.mtime(js_path))
  version <- if (length(mtime) == 1L && !is.na(mtime)) {
    as.character(as.integer(mtime))
  } else {
    "0"
  }
  htmltools::htmlDependency(
    name = name,
    version = version,
    src = c(file = src_dir),
    script = list(src = js_file, defer = ""),
    stylesheet = css_file
  )
}
```

Note on `page_react_html`: `attachDependencies` with `append = TRUE` is used so the dep rides with the static HTML. Verify the dep actually surfaces via `findDependencies` in the test; if `attachDependencies` on a `tagList` of raw `HTML` does not surface, instead return `htmltools::tagList(shinyreact_dep_page(), html)` (deps as a sibling). The test is the source of truth — use whichever passes.

- [ ] **Step 4: Run tests to verify pass**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-page.R")'`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
air format pkg-r/
cd pkg-r && Rscript -e 'devtools::document()' && cd ..
git add pkg-r/R/page.R pkg-r/tests/testthat/test-page.R pkg-r/DESCRIPTION pkg-r/NAMESPACE pkg-r/man
git commit -m "feat(r): page_bare/page_react/page_react_html/page_react_dep (#91)"
```

---

## Task 11: Cross-language wire-format fixtures (Python side)

**Files:**
- Create: `pkg-py/tests/fixtures/wire_format/single_element.json`
- Create: `pkg-py/tests/fixtures/wire_format/nested_tree.json`
- Create: `pkg-py/tests/fixtures/wire_format/empty_children.json`
- Create: `pkg-py/tests/fixtures/wire_format/multi_props.json`
- Create: `pkg-py/tests/fixtures/wire_format/raw_value.json`
- Create: `pkg-py/tests/test_wire_fixtures.py`

These fixtures are the shared source of truth. Python generates and asserts them; R (Task 12) reads and compares semantically.

- [ ] **Step 1: Write `pkg-py/tests/test_wire_fixtures.py`**

```python
import json
from pathlib import Path

import shinyreact

FIXTURES = Path(__file__).parent / "fixtures" / "wire_format"

CASES = {
    "single_element": shinyreact.Node(type="Card", props={"title": "Hello"}),
    "nested_tree": shinyreact.Node(
        type="Card",
        props={"title": "Hi"},
        children=[
            shinyreact.Node(type="Divider"),
            shinyreact.Node(type="Text", props={"value": "x"}),
        ],
    ),
    "empty_children": shinyreact.Node(type="Divider"),
    "multi_props": shinyreact.Node(
        type="TextInput",
        props={"input_id": "name", "label": "Name", "placeholder": "..."},
    ),
}


def _wire(value) -> dict:
    if isinstance(value, shinyreact.Node):
        return value.to_spec().to_dict()
    return value


def test_fixtures_match_committed() -> None:
    for name, value in CASES.items():
        expected = json.loads((FIXTURES / f"{name}.json").read_text())
        assert _wire(value) == expected, name

    # Raw passthrough value (not a Node/Spec).
    raw = {"key": "value", "count": 42}
    expected_raw = json.loads((FIXTURES / "raw_value.json").read_text())
    assert raw == expected_raw
```

- [ ] **Step 2: Generate the fixture files**

Run from repo root:

```bash
uv run python -c '
import json
from pathlib import Path
import shinyreact

out = Path("pkg-py/tests/fixtures/wire_format")
out.mkdir(parents=True, exist_ok=True)

cases = {
    "single_element": shinyreact.Node(type="Card", props={"title": "Hello"}).to_spec().to_dict(),
    "nested_tree": shinyreact.Node(type="Card", props={"title": "Hi"}, children=[
        shinyreact.Node(type="Divider"),
        shinyreact.Node(type="Text", props={"value": "x"}),
    ]).to_spec().to_dict(),
    "empty_children": shinyreact.Node(type="Divider").to_spec().to_dict(),
    "multi_props": shinyreact.Node(type="TextInput", props={"input_id": "name", "label": "Name", "placeholder": "..."}).to_spec().to_dict(),
    "raw_value": {"key": "value", "count": 42},
}
for name, value in cases.items():
    (out / f"{name}.json").write_text(json.dumps(value, indent=2) + "\n")
print("wrote", len(cases), "fixtures")
'
```

- [ ] **Step 3: Run the Python test**

Run: `make py-check-tests` (or `uv run pytest pkg-py/tests/test_wire_fixtures.py -v`)
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add pkg-py/tests/fixtures/wire_format pkg-py/tests/test_wire_fixtures.py
git commit -m "test(py): commit cross-language wire-format fixtures (#91)"
```

---

## Task 12: Cross-language parity test (R side)

**Files:**
- Create: `pkg-r/tests/testthat/test-parity.R`

R builds the equivalent trees, serializes with `.wire_json()`, parses, and compares to the parsed Python fixtures (semantic equality). The fixtures live in `pkg-py/`; the test resolves them relative to the package source via `testthat::test_path()` and a relative path up to the repo root, OR copies them into the R test tree. To avoid cross-package path fragility, **copy** the fixtures into `pkg-r/tests/testthat/fixtures/wire_format/` as part of this task.

- [ ] **Step 1: Copy the fixtures into the R test tree**

Run from repo root:

```bash
mkdir -p pkg-r/tests/testthat/fixtures/wire_format
cp pkg-py/tests/fixtures/wire_format/*.json pkg-r/tests/testthat/fixtures/wire_format/
```

(These are committed copies; a comment in the test notes they must stay in sync with `pkg-py/tests/fixtures/wire_format/`. The `make` target in Task 16 verifies sync.)

- [ ] **Step 2: Write `pkg-r/tests/testthat/test-parity.R`**

```r
# Fixtures are copies of pkg-py/tests/fixtures/wire_format/ — keep in sync.
fixture <- function(name) {
  jsonlite::fromJSON(
    testthat::test_path("fixtures", "wire_format", paste0(name, ".json")),
    simplifyVector = FALSE
  )
}

r_wire <- function(value) {
  jsonlite::fromJSON(shinyreact:::.wire_json(to_spec(value)), simplifyVector = FALSE)
}

test_that("single_element matches Python", {
  expect_equal(
    r_wire(node("Card", props = list(title = "Hello"))),
    fixture("single_element")
  )
})

test_that("nested_tree matches Python", {
  expect_equal(
    r_wire(node(
      "Card",
      node("Divider"),
      node("Text", props = list(value = "x")),
      props = list(title = "Hi")
    )),
    fixture("nested_tree")
  )
})

test_that("empty_children matches Python", {
  expect_equal(r_wire(node("Divider")), fixture("empty_children"))
})

test_that("multi_props matches Python", {
  expect_equal(
    r_wire(node("TextInput", props = list(
      input_id = "name", label = "Name", placeholder = "..."
    ))),
    fixture("multi_props")
  )
})

test_that("raw_value passes through unchanged", {
  expect_equal(
    jsonlite::fromJSON(
      shinyreact:::.wire_json(list(key = "value", count = 42L)),
      simplifyVector = FALSE
    ),
    fixture("raw_value")
  )
})
```

- [ ] **Step 3: Run tests**

Run: `cd pkg-r && Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-parity.R")'`
Expected: PASS. If `empty_children` differs because Python emits `"props": {}` and R emits `"props": []` post-parse (`{}` parses to empty named list, `[]` to empty unnamed list, and `expect_equal` distinguishes them), fix `.wire_json`'s `.as_json_object` so empty props parse back to an empty *named* list matching Python's `{}`. Re-run until green.

- [ ] **Step 4: Commit**

```bash
git add pkg-r/tests/testthat/fixtures pkg-r/tests/testthat/test-parity.R
git commit -m "test(r): cross-language wire-format parity (semantic equality) (#91)"
```

---

## Task 13: `R CMD check` green

**Files:**
- Modify: any `R/*.R` as needed to clear NOTEs/WARNINGs

- [ ] **Step 1: Document and run full check**

Run: `cd pkg-r && Rscript -e 'devtools::document()' && cd .. && make r-check`
Expected: `R CMD check` completes. Acceptable: the one documented NOTE for `shiny:::`/`getFromNamespace` access (per spec). All other NOTEs/WARNINGs/ERRORs must be resolved.

- [ ] **Step 2: Fix anything flagged**

Common fixes:
- Undefined global variables → add to `utils::globalVariables()` in `shinyreact-package.R` or qualify.
- Missing `importFrom` → add roxygen `@importFrom` tags and re-document.
- Undocumented exports → add roxygen and re-document.
- `.Rbuildignore` misses → add patterns.

Re-run `make r-check` until only the accepted `:::` NOTE remains.

- [ ] **Step 3: Run the full test suite via the make target**

Run: `make r-check-tests`
Expected: all testthat tests PASS.

- [ ] **Step 4: Commit**

```bash
air format pkg-r/
git add -A pkg-r
git commit -m "chore(r): clean R CMD check (accepted shiny::: NOTE only) (#91)"
```

---

## Task 14: Example — `examples/app-r/01-hello-world`

**Files:**
- Create: `examples/app-r/01-hello-world/app.R`
- Copy: `examples/app-r/01-hello-world/hello_world.js` (from app-py)
- Copy: `examples/app-r/01-hello-world/styles.css` (from app-py)
- Create: `examples/app-r/01-hello-world/README.md`

- [ ] **Step 1: Copy the shared JS/CSS (React components are language-agnostic)**

Run from repo root:

```bash
mkdir -p examples/app-r/01-hello-world
cp examples/app-py/01-hello-world/hello_world.js examples/app-r/01-hello-world/
cp examples/app-py/01-hello-world/styles.css examples/app-r/01-hello-world/
```

- [ ] **Step 2: Write `examples/app-r/01-hello-world/app.R`**

```r
library(shiny)
library(shinyreact)

src_dir <- normalizePath(dirname(sys.frame(1)$ofile %||% "."), mustWork = FALSE)
`%||%` <- function(a, b) if (is.null(a)) b else a

hello_dep <- htmltools::htmlDependency(
  name = "hello-world",
  version = as.character(as.integer(
    file.mtime(file.path(src_dir, "hello_world.js"))
  )),
  src = c(file = src_dir),
  script = list(src = "hello_world.js", defer = ""),
  stylesheet = "styles.css"
)

# Component helpers — thin wrappers around node() that mirror the registered
# JS components in hello_world.js.
card <- function(title, ...) {
  node("Card", ..., props = list(title = title))
}
text_input <- function(input_id, default_value = "", placeholder = "",
                       label = "") {
  node("TextInput", props = list(
    input_id = input_id, default_value = default_value,
    placeholder = placeholder, label = label
  ))
}
hr_node <- function() node("Divider")
input_display <- function(input_id, default_value = "", label = "") {
  node("InputDisplay", props = list(
    input_id = input_id, default_value = default_value, label = label
  ))
}
output_display <- function(output_id, label = "") {
  node("OutputDisplay", props = list(output_id = output_id, label = label))
}

ui <- page_react(
  hello_dep,
  ui_output("hello")
)

server <- function(input, output, session) {
  output$hello <- render_reactive({
    card(
      "Hello Shiny React!",
      text_input(
        "txtin", "Hello, world!",
        placeholder = "Enter your message here...",
        label = "Type something to send to Shiny server:"
      ),
      hr_node(),
      input_display("txtin", default_value = "Hello, world!",
                    label = "Client-side value:"),
      output_display("txtout", label = "Response from Shiny server:")
    )
  })

  output$txtout <- render_reactive({
    toupper(input$txtin)
  })
}

shinyApp(ui, server)
```

Note: the `src_dir`/`%||%` ordering above defines `%||%` after use — move the `%||%` definition to the **top** of the file. Final order: `library()` calls, then `%||%`, then `src_dir`, then the rest. Apply that ordering when writing the file.

- [ ] **Step 3: Write `examples/app-r/01-hello-world/README.md`**

```markdown
# 01 — Hello World (app.R pattern)

R port of `examples/app-py/01-hello-world`. The server describes the UI as a
`node()` tree rendered by registered React components in `hello_world.js`.

## Run

```r
shiny::runApp("examples/app-r/01-hello-world")
```
```

- [ ] **Step 4: Smoke-test that the app object builds**

Run: `cd examples/app-r/01-hello-world && Rscript -e 'shiny::shinyAppFile("app.R")'`
Expected: prints a `shiny.appobj` without error (does not start the server). If `ofile` is `NULL` under `Rscript`, the `src_dir` fallback to `"."` applies — run from the example directory so relative asset paths resolve.

- [ ] **Step 5: Commit**

```bash
air format examples/app-r/01-hello-world/app.R
git add examples/app-r/01-hello-world
git commit -m "docs(r): example 01-hello-world (app.R pattern) (#91)"
```

---

## Task 15: Examples — `02-inputs`, `04-messages`, `ui-tsx-r/01-hello`

**Files:**
- Create: `examples/app-r/02-inputs/{app.R,README.md}` + copied JS/CSS
- Create: `examples/app-r/04-messages/{app.R,README.md}` + copied JS/CSS
- Create: `examples/ui-tsx-r/01-hello/{app.R,www/index.html,README.md}`

- [ ] **Step 1: Port `02-inputs`**

Copy JS/CSS from `examples/app-py/02-inputs/`, then translate `app.py` to `app.R` following the Task 14 pattern: `node()` helpers for each input type, `render_reactive()` per output. Enable bookmarking to double as the bookmark demo — add at the end of `app.R`:

```r
enableBookmarking("url")
shinyApp(ui, server)
```

Read `examples/app-py/02-inputs/app.py` for the exact component types and props; replicate each helper. Smoke-test as in Task 14 Step 4.

- [ ] **Step 2: Port `04-messages`**

Copy JS/CSS from `examples/app-py/04-messages/`. Translate the server's `send_message()` usage:

```r
observeEvent(input$send, {
  shinyreact::send_message(session, "notification",
                           list(text = "Hello!", level = "info"))
})
```

Follow the app-py message types exactly. Smoke-test.

- [ ] **Step 3: Port `ui-tsx-r/01-hello`**

Copy `www/` from `examples/ui-tsx/01-hello/` (the static React client is language-agnostic). Write `app.R`:

```r
library(shiny)
library(shinyreact)

ui <- page_react_html("www/index.html")

server <- function(input, output, session) {
  output$value <- render_reactive({
    list(message = paste("Server received:", input$txt))
  })
}

shinyApp(ui, server)
```

Adjust output/input IDs to match the ui-tsx example's `useShinyInput`/`useShinyOutputValue` IDs (read `examples/ui-tsx/01-hello/www/`). Smoke-test that the app object builds.

- [ ] **Step 4: Commit**

```bash
air format examples/app-r examples/ui-tsx-r
git add examples/app-r/02-inputs examples/app-r/04-messages examples/ui-tsx-r/01-hello
git commit -m "docs(r): examples 02-inputs, 04-messages, ui-tsx 01-hello (#91)"
```

---

## Task 16: Docs, fixture-sync check, spec erratum, final verification

**Files:**
- Modify: `docs/features.md`
- Modify: `README.md`
- Create: `pkg-r/README.md`
- Create: `pkg-r/_pkgdown.yml`
- Modify: `docs/superpowers/specs/2026-05-26-r-package-design.md` (parity wording)
- Modify: `Makefile` (fixture-sync check — optional but recommended)
- Modify: `docs/todos.md`

- [ ] **Step 1: Update the spec's parity wording**

Edit `docs/superpowers/specs/2026-05-26-r-package-design.md`: replace "byte-for-byte" / "byte-equality" phrasing in the "Cross-language wire-format parity" and Bookmarking sections with **semantic JSON equality** (parse both, compare structures), noting Python `json.dumps` and R `jsonlite` differ in whitespace by design. Also note the render path returns a plain list (Shiny serializes) and `toJSON` is not on the output critical path.

- [ ] **Step 2: Add an R section to `docs/features.md`**

Add a section mirroring the Python tables, marking each row Working. Cover `node`/`spec`/`element`, `to_spec`, `ui_output`, `render_reactive`, `page_react`/`page_bare`/`page_react_html`/`page_react_dep`, `send_message`, bookmarking. Note JS bridge hooks are accessed from R apps identically (the same bundle on `window.shinyreact`).

- [ ] **Step 3: Add an R section to `README.md`** pointing at `examples/app-r/01-hello-world/` and `pkg-r/`.

- [ ] **Step 4: Write `pkg-r/README.md`**

```markdown
# shinyreact (R)

Shiny UI infrastructure for JSON-driven React rendering. Pure plumbing — no UI
components. See `docs/features.md` for the full feature inventory.

## Install (dev)

```r
pak::local_install("pkg-r")
```

## Hello world

See `examples/app-r/01-hello-world/`.
```

- [ ] **Step 5: Write `pkg-r/_pkgdown.yml`**

```yaml
url: https://posit-dev.github.io/shinyreact/r
template:
  bootstrap: 5
reference:
  - title: Pages
    contents: [page_react, page_bare, page_react_html, page_react_dep]
  - title: Outputs
    contents: [ui_output, render_reactive]
  - title: Data model
    contents: [node, spec, element, to_spec]
  - title: Messaging
    contents: [send_message]
```

- [ ] **Step 6: Add a fixture-sync check to the `Makefile`**

Add a target and wire it into `r-check`:

```makefile
.PHONY: r-check-fixtures
r-check-fixtures:  ## [r] Verify R wire-format fixtures match Python's
	@echo "🔁 Checking wire-format fixture sync"
	diff -r pkg-py/tests/fixtures/wire_format pkg-r/tests/testthat/fixtures/wire_format
```

Add `r-check-fixtures` to the `r-check` prerequisite list.

- [ ] **Step 7: Remove the resolved entry from `docs/todos.md`** if any references the R package placeholder; otherwise skip.

- [ ] **Step 8: Render docs to confirm pkgdown config is valid**

Run: `make r-docs-render`
Expected: produces a non-empty site under `pkg-r/docs/` without error. (If `make r-docs-render` requires network/setup unavailable here, run `cd pkg-r && Rscript -e 'pkgdown::check_pkgdown()'` instead and record the result.)

- [ ] **Step 9: Final full verification**

Run: `make r-check && make py-check-tests`
Expected: R checks pass (accepted `:::` NOTE only); Python tests pass (fixtures intact).

- [ ] **Step 10: Commit**

```bash
air format pkg-r/
git add docs/features.md README.md pkg-r/README.md pkg-r/_pkgdown.yml Makefile docs/todos.md docs/superpowers/specs/2026-05-26-r-package-design.md
git commit -m "docs(r): features, README, pkgdown, fixture-sync, spec erratum (#91)"
```

---

## Self-Review Notes

**Spec coverage:**
- S7 `Spec`/`Element`/`Node` + snake_case constructors + validators → Tasks 2
- `to_spec()` generic + downstream extension via `S7::method` → Task 3 (+ doc), Task 5 default method
- Wire serializer + empty-object handling → Task 4
- `shinyreact_dep()` mtime version + `packageVersion()` fallback → Task 5
- Per-output vs page-level dep split → Tasks 5, 10
- `ui_output()` + `extra_deps` → Task 6
- `render_reactive()` returns plain list (Shiny serializes) → Task 7
- `send_message()` + module namespacing → Task 8
- Bookmarking via `shiny:::` wrapper + restore script encoding + security → Task 9
- `page_bare`/`page_react`/`page_react_html`/`page_react_dep` → Task 10
- Cross-language parity (semantic equality) + fixtures → Tasks 11, 12, 16 (sync check)
- `R CMD check` green with accepted `:::` NOTE → Task 13
- Examples (01-hello-world, 02-inputs, 04-messages, ui-tsx 01-hello) → Tasks 14, 15
- Bookmark demo via `enableBookmarking("url")` in 02-inputs → Task 15
- Docs (features.md, README, pkg-r/README, pkgdown) → Task 16
- Spec erratum (parity wording, render path) → Task 16

**Deviations from spec (documented in plan + Task 16 updates the spec):**
1. Parity is **semantic JSON equality**, not byte-for-byte (whitespace differs by design).
2. Render path returns a plain list and lets Shiny serialize; `toJSON`/`.wire_json` is for tests + bookmark payload only, not the output critical path.

**Open item carried into execution:** Task 9 Step 1 spike must confirm the exact `shiny` internal symbol for restore-context access; the code blocks contain a clearly-marked PLACEHOLDER to replace with the spike result.

**Type/name consistency:** `to_spec`, `.wire_json`, `.render_transform`, `shinyreact_dep`, `shinyreact_dep_page`, `restore_script_tag`, `.restore_input_values` are used consistently across tasks. `node()` children via `...`; `element()` children as key list — matches Task 2 definitions throughout.
